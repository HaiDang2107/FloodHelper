import 'package:flutter/material.dart';

class MessagesSheet extends StatelessWidget {
  const MessagesSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Messages Content Placeholder'),
    );
  }
}
