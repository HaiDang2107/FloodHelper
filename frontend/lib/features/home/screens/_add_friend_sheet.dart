import 'package:flutter/material.dart';

class AddFriendSheet extends StatelessWidget {
  const AddFriendSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add Friend Content Placeholder'),
    );
  }
}
