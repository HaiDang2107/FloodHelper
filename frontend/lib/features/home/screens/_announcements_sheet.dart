import 'package:flutter/material.dart';

class AnnouncementsSheet extends StatelessWidget {
  const AnnouncementsSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Announcements Content Placeholder'),
    );
  }
}
