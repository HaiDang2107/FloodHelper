import 'package:flutter/material.dart';

class SearchPersonSheet extends StatelessWidget {
  const SearchPersonSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Search Person Content Placeholder'),
    );
  }
}
